{
    "functions": {
        "api/index.go": {
            "runtime": "go1.x"
        }
    },
    "routes": [
        { "src": "/api/(.*)", "dest": "/api/index.go" },
        { "src": "/(.*)", "dest": "/static/$1" }
    ]
}
